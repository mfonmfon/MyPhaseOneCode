//this is the code for 2.17

import java.util.scanner;
public class Arithmetic{

   public static void main(String[] args) {

   scanner input = new scanner(system.in);

   system.out.print("Enter first integer");
   int firstNumber = input.nextln();

   system.out.print("Enter third integer");
   int thirdNumber= input.nextln();

   system.out.print("Enter second integer");
   int secondNumber = input.nextln();
   


   int sum = firstNumber + seconNumber + thirdNumber;

   system.out.printf("sum is %d%n", sum);

   int product = firstNumber * secondNumber * thirdNumber;
   system.out.printf("product is %d%n", product);

   if (firstNumber == secondNumber);
   system.out.printf("%d == %d%n", firstNumber == secondNumber);

   if (firstNumber != secondNumber);
   system.out.printf(%d == %d%n, firstNumber != secondNumber);

   if (firstNumber < secondNumber);
   system.out.printf("%d < %d%n", firstNumber < secondNumber);


   if (firstNumber >  secondNumber);
   system.out.printf("d% > %d%n", firstNumber < secondNumber);

   }
}




  